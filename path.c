#include "simpleshell.h"

/**
 * sh_get_path - search if a file is executable according the PATH variable
 *
 * @env: the environment array
 *
 * Return: 0 if non executable, else, executes it
 */

int sh_get_path(char **env, char **cmd)
{
	
}
